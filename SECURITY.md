# 安全政策 (Security Policy)

## 支持版本

| 版本 | 安全维护状态 |
|------|--------------|
| 3.1.x (main) | ✅ 积极维护 |
| < 3.1 | ❌ 不再维护 |

## 漏洞报告（私有披露）

**请勿通过公开 GitHub Issue 报告安全漏洞。**

请改用以下方式**私有**披露：

- 在仓库的 **Security → Report a vulnerability** 中提交（GitHub 私有安全公告）
- 或发送邮件至安全联系人（见下方占位，请维护者补充）

> ⚠️ 维护者请在发布前将下方邮箱替换为真实安全联系地址。

```
security@example.com
```

我们会在 **72 小时内** 确认收到，并在 **7 个工作日内** 给出初步评估与修复时间表。

## 安全设计要点

本项目在开源成熟度梳理中已落实以下安全约束：

- **P0-03 网络隔离**：单元测试与集成测试默认禁用 socket（`pytest-socket`），
  仅 `tests/smoke/` 下的真实数据源连通性测试需联网。
- **P0-04 TLS**：所有对外 HTTPS 请求均使用默认证书校验；仓库通过
  `gitleaks` + `pip-audit` 持续扫描密钥泄漏与依赖漏洞。
- **密钥管理**：API key 等敏感配置仅通过环境变量（`.env`，已在 `.gitignore`）
  或 `QUANT_` 前缀注入；仓库内 `.env.example` 仅为占位，不含真实凭据。
- **交易安全**：默认运行模式为**研究 / 模拟**；任何实盘交易能力均为独立、显式
  启用的适配器，且分析默认只读、下单需显式调用 `/api/execute`。

## 依赖安全

依赖漏洞由 Dependabot 与 `pip-audit` 自动扫描；代码安全由 CodeQL 持续分析。
